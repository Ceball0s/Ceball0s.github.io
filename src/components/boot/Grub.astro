<div class="grub-layout">
  <div class="grub-header">GNU GRUB version 2.02~beta2-9ubuntu1</div>

  <div class="grub-main-content">
    <div class="grub-box">
      <div class="entry selected">Ubuntu</div>
      <div class="entry">Advanced options for Ubuntu</div>
      <div class="entry">Memory test (memtest86+)</div>
      <div class="entry">Memory test (memtest86+, serial console 115200)</div>
    </div>

    <div class="grub-footer">
      <p>Use the ↑ and ↓ keys to select which entry is highlighted.</p>
      <p>Press enter to boot the selected OS, `e` to edit the commands</p>
      <p>before booting or `c` for a command-line.</p>
    </div>
  </div>
</div>

<style>
  .grub-layout {
    display: flex;
    flex-direction: column;
    height: 100vh;
    color: white;
    /* Reducimos el margen superior inicial (de 65px a 30px) */
    padding: 30px 0 20px 0; 
    font-family: "Ubuntu Mono", monospace;
    box-sizing: border-box;
  }

  .grub-header {
    text-align: center;
    /* Margen pequeño debajo del título para que la caja esté cerca */
    margin-bottom: 5px; 
    font-size: 1.1rem;
  }

  .grub-main-content {
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start; 
    /* Reducimos el espacio entre título y caja (de 6vh a 2vh) */
    padding: 2vh 40px 0 40px; 
  }

  .grub-box {
    width: 100%;
    max-width: 900px;
    /* Mantenemos la caja grande para el efecto de "vacío" */
    height: 55vh; 
    border: 2px solid white;
    padding: 12px 0;
    box-sizing: border-box;
  }

  .entry {
    padding: 2px 35px;
    font-size: 18px;
    position: relative;
  }

  .entry.selected {
    background: #aaaaaa;
    color: #000;
  }

  .entry.selected::before {
    content: "*";
    position: absolute;
    left: 12px;
  }

  .grub-footer {
    width: 100%;
    max-width: 900px;
    margin-top: 35px;
    text-align: center;
    font-size: 15px;
    line-height: 1.3;
  }

  .grub-footer p {
    margin: 4px 0;
  }
</style>