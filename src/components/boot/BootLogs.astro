<pre id="bootlogs" class="logs"></pre>

<style is:global>
  .logs {
    background-color: #000;
    padding: 25px;
    margin: 0;
    white-space: pre-wrap;
    font-family: 'JetBrains Mono', monospace; /* O cualquier mono que uses */
    font-size: 16px;
    line-height: 1.5;
  }

  /* El verde con brillo (glow) */
  .ok {
    color: #00ff5f !important;
    font-weight: bold;
    text-shadow: 0 0 5px rgba(0, 255, 95, 0.5);
  }

  /* Los corchetes y texto secundario */
  .bracket, .desc {
    color: #888888 !important;
  }

  /* El nombre del servicio resaltado en blanco puro */
  .service {
    color: #ffffff !important;
    font-weight: 500;
  }
</style>